use std::io;

fn main() {
    // loop {
        let mut input = String::new();
        io::stdin().read_line(&mut input);
        let mut a = Ok(input.trim_right());
        // let s = number.unwrap().parse::<i32>().expect("ba no");

        // let numberr = number.parse::<i32>().expect("ss");
        // println!("{}", numberr);
    // }
}
